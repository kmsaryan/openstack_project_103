### Step-by-Step Guide to Verify Each Service

#### 1. **Grafana (Bastion Host)**
- **Host**: `test3_bastion` (89.46.80.185)
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa ubuntu@89.46.80.185
  systemctl status grafana-server
  ```
- **Access Grafana**:
  Open a web browser and navigate to `http://89.46.80.185:3000`. The default login credentials are `admin/admin`.

#### 2. **Prometheus (Bastion Host)**
- **Host**: `test3_bastion` (89.46.80.185)
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa ubuntu@89.46.80.185
  systemctl status prometheus
  ```
- **Access Prometheus**:
  Open a web browser and navigate to `http://89.46.80.185:9090`.

#### 3. **HAProxy (Main Proxy)**
- **Host**: `test3_HAproxy` (185.52.156.35)
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@185.52.156.35
  systemctl status haproxy
  ```
- **Check Configuration**:
  ```sh
  haproxy -c -f /etc/haproxy/haproxy.cfg
  ```

#### 4. **Nginx (Main Proxy)**
- **Host**: `test3_HAproxy` (185.52.156.35)
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@185.52.156.35
  systemctl status nginx
  ```

#### 5. **Keepalived (Main Proxy)**
- **Host**: `test3_HAproxy` (185.52.156.35)
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@185.52.156.35
  systemctl status keepalived
  ```

#### 6. **SNMPd (Main Proxy)**
- **Host**: `test3_HAproxy` (185.52.156.35)
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@185.52.156.35
  systemctl status snmpd
  ```

#### 7. **HAProxy (Standby Proxy)**
- **Host**: `test3_HAproxy2` (185.52.156.20)
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@185.52.156.20
  systemctl status haproxy
  ```
- **Check Configuration**:
  ```sh
  haproxy -c -f /etc/haproxy/haproxy.cfg
  ```

#### 8. **Nginx (Standby Proxy)**
- **Host**: `test3_HAproxy2` (185.52.156.20)
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@185.52.156.20
  systemctl status nginx
  ```

#### 9. **Keepalived (Standby Proxy)**
- **Host**: `test3_HAproxy2` (185.52.156.20)
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@185.52.156.20
  systemctl status keepalived
  ```

#### 10. **SNMPd (Standby Proxy)**
- **Host**: `test3_HAproxy2` (185.52.156.20)
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@185.52.156.20
  systemctl status snmpd
  ```

#### 11. **Node Exporter (All Hosts)**
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa ubuntu@89.46.80.185
  systemctl status node_exporter
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@185.52.156.35
  systemctl status node_exporter
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@185.52.156.20
  systemctl status node_exporter
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@10.10.0.28
  systemctl status node_exporter
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@10.10.0.30
  systemctl status node_exporter
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@10.10.0.19
  systemctl status node_exporter
  ```

#### 12. **Flask App (Dev Servers)**
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@10.10.0.28
  systemctl status flask_app
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@10.10.0.30
  systemctl status flask_app
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@10.10.0.19
  systemctl status flask_app
  ```

#### 13. **SNMPd (Dev Servers)**
- **Verify Installation**:
  ```sh
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@10.10.0.28
  systemctl status snmpd
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@10.10.0.30
  systemctl status snmpd
  ssh -i /home/madhav/.ssh/id_rsa -o ProxyJump=ubuntu@89.46.80.185 ubuntu@10.10.0.19
  systemctl status snmpd
  ```

### Configuring the Grafana Dashboard

1. **Access Grafana**:
   Open a web browser and navigate to `http://89.46.80.185:3000`. Log in with the default credentials `admin/admin`.

2. **Add Data Source**:
   - Click on the gear icon (⚙️) in the left sidebar to open the Configuration menu.
   - Click on "Data Sources".
   - Click on "Add data source".
   - Select "Prometheus" from the list.
   - Set the URL to `http://localhost:9090` (assuming Prometheus is running on the same host as Grafana).
   - Click "Save & Test" to verify the connection.

3. **Create a Dashboard**:
   - Click on the "+" icon in the left sidebar and select "Dashboard".
   - Click on "Add new panel".
   - In the "Query" section, select "Prometheus" as the data source.
   - Write a query to fetch the desired metrics (e.g., `up` to check if the nodes are up).
   - Select a visualization type (e.g., Graph, Gauge).
   - Configure the panel options (e.g., title, description).
   - Click "Apply" to add the panel to the dashboard.

4. **Save the Dashboard**:
   - Click on the save icon (💾) in the top right corner.
   - Enter a name for the dashboard and click "Save".

### Example Queries for Grafana Panels

- **Node Availability**:
  ```prometheus
  up
  ```
- **CPU Usage**:
  ```prometheus
  node_cpu_seconds_total{mode="idle"}
  ```
- **Memory Usage**:
  ```prometheus
  node_memory_MemAvailable_bytes
  ```

By following these steps, you can verify the installed services and configure a Grafana dashboard to monitor the health and